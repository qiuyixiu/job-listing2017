<%= simple_form_for [:admin, @job] do |f| %>
  <div class="form-job">
    <%= f.input :title %>
    <%= f.input :description %>
  </div>
  <%= f.submit "Submit", class: "btn btn-primary", data: { disable_wtih: "Submiting..."} %>
<% end %>
